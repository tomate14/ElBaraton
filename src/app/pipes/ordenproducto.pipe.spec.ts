import { OrdenproductoPipe } from './ordenproducto.pipe';

describe('OrdenproductoPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenproductoPipe();
    expect(pipe).toBeTruthy();
  });
});
