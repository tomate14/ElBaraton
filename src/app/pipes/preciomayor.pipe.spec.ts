import { PreciomayorPipe } from './preciomayor.pipe';

describe('PreciomayorPipe', () => {
  it('create an instance', () => {
    const pipe = new PreciomayorPipe();
    expect(pipe).toBeTruthy();
  });
});
